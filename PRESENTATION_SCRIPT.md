# 低空目标识别项目 3 分钟汇报稿

## 一、汇报稿正文

各位老师、同学大家好，我们组当前选择的题目是“低空目标识别”。

这个项目的目标是面向无人机低空飞行场景，研究在远距离、小目标、快速运动条件下的目标识别问题。我们的整体思路不是一开始就直接做算法，而是先把完整的仿真平台搭建起来，再在这个平台上接入视觉识别算法。

目前我们已经完成了项目的第一阶段，也就是基础环境和仿真链路的搭建。具体来说，我们在 Windows 11 环境下，使用 native Ubuntu 搭建了 Ubuntu 24.04 的 Linux 开发环境，并将 WSL 迁移到了 D 盘，解决了系统盘空间占用的问题。

在此基础上，我们重新下载并配置了 PX4 飞控源码，最终选用了稳定版 `PX4 v1.16.0`。之所以选择这个版本，是因为它的稳定性更好，而且与 `Gazebo Harmonic` 的兼容性更高，更适合课程项目快速落地。

然后，我们完成了 PX4 依赖、Gazebo Harmonic 仿真环境以及相关开发库的安装，解决了包括 GitHub 下载慢、Gazebo 依赖缺失、OpenCV 缺失以及 QGroundControl 无法连接等问题。

截至目前，我们已经成功运行了以下完整链路：

`PX4 SITL + Gazebo Harmonic + QGroundControl`

也就是说，现在仿真无人机已经可以成功启动，Gazebo 中能够正常加载 `x500` 无人机模型，PX4 飞控也已经和 QGroundControl 地面站建立了通信。日志中已经出现了“Gazebo world is ready”“Spawning model”“Ready for takeoff”等关键信息，这说明我们的仿真平台主链路已经打通。

在技术栈方面，目前已经实际使用的主要技术包括：

- Windows 11
- native Ubuntu
- Ubuntu 24.04
- PX4 v1.16.0
- Gazebo Harmonic
- QGroundControl
- MAVLink
- Python
- OpenCV

后续准备接入的核心算法是 `YOLOv8`，后面还可以根据进度进一步扩展 `ByteTrack` 做目标跟踪。

我们当前的项目进度可以概括为一句话：已经完成了低空目标识别项目的基础仿真平台搭建，下一步将进入仿真相机图像获取和视觉检测算法接入阶段。

接下来我们计划优先完成三件事：

第一，获取 Gazebo 中的仿真相机图像；  
第二，接入 YOLOv8 做目标检测；  
第三，完成基础实验结果展示。

我们的目标是先把“看得见、识得出”做好，再根据时间情况决定是否继续扩展目标跟踪和闭环控制。

我的汇报完毕，谢谢大家。

## 二、极简版发言提纲

如果现场时间更短，可以按下面这个版本讲：

1. 我们组选题是“低空目标识别”，目标是在无人机低空场景下做目标检测。
2. 目前已经完成 `native Ubuntu + Ubuntu + PX4 v1.16.0 + Gazebo Harmonic + QGroundControl` 的环境搭建。
3. 已经成功启动仿真无人机，并且地面站通信已经打通。
4. 当前技术栈主要包括 PX4、Gazebo、QGroundControl、Python 和 OpenCV。
5. 下一步将获取仿真相机图像，并接入 YOLOv8 进行目标检测。

## 三、老师追问时可用的补充回答

### 1. 你们现在做到哪一步了？

可以回答：

目前已经完成了项目最核心的前期准备工作，仿真平台已经搭起来了，PX4、Gazebo 和 QGroundControl 都已经跑通。下一步是获取相机图像并接入目标检测算法。

### 2. 你们为什么先做环境搭建？

可以回答：

因为这个题目不仅仅是单纯做图像分类或目标检测，它更强调低空无人机场景下的系统验证，所以必须先把仿真平台搭起来，后续的视觉算法才能真正落到无人机场景里。

### 3. 你们为什么没有先接 ROS？

可以回答：

因为当前阶段的目标是先把主链路跑通，也就是仿真、通信和视觉输入。ROS 不是必须项，后续如果需要做更复杂的模块化集成，再考虑扩展 ROS2。

### 4. 你们后面准备做什么？

可以回答：

后面主要做三件事：获取仿真相机图像、接入 YOLOv8、完成检测效果展示。如果进度允许，再扩展跟踪或控制部分。
